# Exemplo (NÃO coloque segredos aqui no repo)
# Copie para .env e preencha localmente.

JWT_SECRET=troque-por-um-segredo-forte
ADMIN_TOKEN=troque-por-um-token-forte

# Se tiver DB via docker-compose, normalmente não precisa expor aqui.
